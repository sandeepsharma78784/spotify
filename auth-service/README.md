mvn clean package [while doing this make sure strong net connection , coz it will download 			jar mentioned in pom.xml]
then run java -jar target/your-app.jar 
authentication-1.0.jar

java -jar target/authentication-1.0.jar