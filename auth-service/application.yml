spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    driver-class-name: org.postgresql.Driver
    username: user1
    password: Sandeeppeednas@1
  jpa:
    hibernate:
      ddl-auto: update   # dev ke liye (prod me mat use karna)
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always  # agar schema.sql/data.sql chalana ho



